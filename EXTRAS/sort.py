# a fuction to remove duplicate records from an arra(list)
# a function to sort numbers from largest to smallest 
# to separate types in a list to a dictinary that has all of the arranged like {int: [1,2,4], str : ['John','Bean']